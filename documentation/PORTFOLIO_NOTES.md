# Portfolio Notes

The public web interface is intentionally read-only and sanitized. The original database model contains fields for restricted discovery sites, sensitive notes, contact information, holdings/storage, and transfer logistics. Those fields belong in the relational model for demonstrating data architecture, but are not exposed through the GitHub Pages client.

The desktop application source is preserved to show project evolution: an earlier Tkinter implementation and a later Flet implementation.
