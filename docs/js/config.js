// GitHub Pages client configuration.
// The Supabase PUBLISHABLE key (or legacy anon key) is intended for browser use when RLS is configured.
// NEVER put a Supabase service_role key or secret key here.
window.APP_CONFIG = {
  supabaseUrl: "",
  supabasePublishableKey: "",
  repoUrl: "https://github.com/kristinealli/nagpra-repatriation-db"
};
